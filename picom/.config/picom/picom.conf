backend = "glx";
vsync = true;
glx-use-copysubbuffer-mesa = true;

# These options are often deprecated or causing issues in newer picom versions
# but I'll keep them cleaned up if you want them.
# glx-copy-from-front = true; 
# use-damage = true;
